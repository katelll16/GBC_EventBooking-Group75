package com.group75.bookingservice.dto;

public record BookingRequest(
        Long id,
        String bookingName,
        String eventId,
        String userId
        userDetails.userDetails){}

public rcord userDetails(String email, String firstName, String lastName){}

