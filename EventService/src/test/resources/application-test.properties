# Test environment settings
server.port=8082
spring.application.name=EventService-Test

# MongoDB (for testing)
spring.data.mongodb.uri=mongodb://localhost:27018/eventservice_test_db

# PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/eventservice_test_db
# spring.datasource.username=test_username
# spring.datasource.password=test_password
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Logging level for tests
logging.level.org.springframework=ERROR
logging.level.com.group75=DEBUG
