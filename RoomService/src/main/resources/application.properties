# RoomService application properties
spring.application.name=RoomService
server.port=8083

# MongoDB connection settings (adjust these according to your MongoDB setup)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=roomservice_db

