# RoomService test properties
spring.application.name=RoomServiceTest
server.port=0  # Allow server to use a random port during tests

# MongoDB test database settings
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=roomservice_test_db

# H2 Database settings
# spring.datasource.url=jdbc:h2:mem:roomservice_test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
